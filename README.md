# Keyword Search

> 🔍 Поиск ключевых слов в файлах с поддержкой регулярных выражений

Консольное приложение для поиска ключевых слов (или regex-паттернов) по выбранной директории. Результаты сохраняются в лог-файл.

---

## ✨ Возможности

- Выбор папки через графический диалог
- Поиск по ключевому слову или регулярному выражению
- Сохранение результатов в `.log` файл
- Поддержка UTF-8

---

## 📋 Требования

- **Python 3.8+**
- **tkinter** (обычно идёт в комплекте с Python)

---

## 🚀 Установка и запуск

### Вариант 1: Python venv

```bash
# Клонируйте репозиторий (или перейдите в папку проекта)
cd HW

# Создайте виртуальное окружение
python3 -m venv .venv

# Активируйте venv
# macOS / Linux:
source .venv/bin/activate
# Windows:
# .venv\Scripts\activate

# Запуск
python main.py
```

### Вариант 2: Nix Shell

```bash
# Перейдите в папку проекта
cd HW

# Войдите в nix-shell (подтянет Python + tkinter)
nix-shell

# Запуск
python main.py
```

> 💡 В `shell.nix` уже настроены Python 3 с tkinter, git и gh.

---

## 📖 Использование

1. Выберите папку для поиска (откроется диалог)
2. Введите ключевое слово или regex
3. Укажите путь для сохранения `keyword_search.log`
4. Ответьте `y` или `n` на вопрос про регулярные выражения
5. Результаты появятся в консоли и в лог-файле

---

## 📁 Структура проекта

```
HW/
├── main.py          # Точка входа
├── shell.nix        # Nix окружение
├── README.md
└── src/
    ├── searcher.py  # Логика поиска
    └── utils.py     # Диалоги (tkinter)
```

---

## 📄 Лицензия

MIT
